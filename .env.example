# Set to "false" once the RPOS backend is running to use the real API.
NEXT_PUBLIC_USE_MOCK=true

# Base URL of the RPOS backend (FastAPI). The client appends /v1 automatically.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
