# The only audit warnings here are dev-only build/electron transitive deps.
# `npm audit fix --force` "fixes" them by downgrading Next/ESLint and breaks the
# app. Silence the nag so nobody is tempted. Check deps deliberately, not via --force.
audit=false
fund=false
